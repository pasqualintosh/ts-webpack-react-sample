import React from 'react';

interface IProps {}
interface IState {}

const Home: React.FC<IProps> = (): JSX.Element => {
  React.useEffect(() => {}, []);

  return <div></div>;
};

export default Home;
